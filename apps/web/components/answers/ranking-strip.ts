/**
 * Rank stays put; airport, composite and lamp wrap in a narrow chat column.
 * Shared by the ranking table and the pending row so those two strips cannot
 * drift apart.
 */
export const RANKING_STRIP = "flex flex-wrap items-center gap-x-3 gap-y-2 px-3.5";

export const RANKING_RANK = "w-[26px] shrink-0";

export const RANKING_AIRPORT = "min-w-0 flex-1 basis-40";
